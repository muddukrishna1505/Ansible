- name: About filters concept
  hosts: local
  connection: local
  vars:
    topics: ansible, shell, terraform, python
  tasks:
  - name: converting string values into list
    debug:
      msg: "{{ topics | split (',') }}"
      

