- name: install nginx
  hosts: node
  become: yes
  tasks:
    - name: installing nginx on Rhel
      ansible.builtin.dnf:
        name: nginx
        state: present

    - name: Starting the nginx service
      ansible.builtin.service:
        name: nginx
        state: started
        enabled: true
      
        


        

        