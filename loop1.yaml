- name: Demo on loop
  hosts: local
  connection: local

  tasks:
  - name: print names
    debug: 
      msg: " Hi {{ item }} " # item is reserved special variable

    loop:
    - Raju
    - krishna
    - hemansh
    - karthi
    - haneesh   